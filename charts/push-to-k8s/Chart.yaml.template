apiVersion: v2
name: push-to-k8s
description: A Kubernetes controller that automatically synchronizes labeled secrets from a source namespace to all other namespaces in the cluster

# Application chart for deploying the push-to-k8s controller
type: application

# Chart version - dynamically set by CI/CD
version: ${CHART_VERSION}

# Application version - matches the push-to-k8s controller version
appVersion: ${APP_VERSION}

keywords:
  - kubernetes
  - secrets
  - sync
  - automation
  - controller

home: https://github.com/supporttools/push-to-k8s
sources:
  - https://github.com/supporttools/push-to-k8s

maintainers:
  - name: mattmattox
    email: mmattox@support.tools

# Optional: icon URL
# icon: https://cdn.support.tools/push-to-k8s/logo.png
